# Spring security login form Demo

## Run in using im-memory user
```
mvn clean spring-boot:run -Dspring.profiles.active=inmemorysec
```
 
## Run in using im-memory DB
```
mvn clean spring-boot:run -Dspring.profiles.active=dbsec
```